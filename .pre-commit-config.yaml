repos:
  - repo: local
    hooks:
      - id: push-gate
        name: push-gate
        language: system
        entry: python scripts/ci/run_push_gate.py --python --skills
        pass_filenames: false
        stages: [pre-push]
